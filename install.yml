# Setup is required before running this, see the README.md file

---
- hosts: new-system
  gather_facts: True
  roles: 
    - role: initial-steps
      tags: initial-steps
    - role: disk-prep
      tags: disk-prep
    - role: installation
      tags: installation
    - role: configuration
      tags: configuration
